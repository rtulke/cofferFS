name: Release

# build+verify run on every push/PR to main so packaging breakage is caught
# early; the release job only fires on an actual "vX.Y.Z" tag push.
on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  CARGO_DEB_VERSION: "3.7.0"

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - id: debian12
            image: debian:12-slim
          - id: debian13
            image: debian:13-slim
          - id: ubuntu2404
            image: ubuntu:24.04
          - id: ubuntu2604
            image: ubuntu:26.04
    runs-on: ubuntu-latest
    container: ${{ matrix.image }}
    steps:
      # actions/checkout shells out to git, which isn't present in the bare
      # distro images used as build containers - install it (and everything
      # the Rust build itself needs) before checking out.
      - name: Install build prerequisites
        run: |
          apt-get update -qq
          DEBIAN_FRONTEND=noninteractive apt-get install -y -qq \
            git build-essential pkg-config perl libfuse3-dev curl ca-certificates

      - uses: actions/checkout@v7

      # rustup's toolchain, not the distro's apt rustc/cargo (1.63-1.75
      # depending on release - too old for several dependencies, which need
      # the 2024 edition). See README.md.
      - name: Install Rust
        run: |
          curl --proto "=https" --tlsv1.2 -sSf https://sh.rustup.rs | \
            sh -s -- -y --default-toolchain stable --profile minimal
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"

      # One .deb per distro, built natively in that distro's own container -
      # not one "universal" package. Debian 12/Ubuntu 24.04 ship libfuse3 as
      # SONAME 3 (libfuse3-3); Debian 13/Ubuntu 26.04 bumped it to SONAME 4
      # (libfuse3-4, with no libfuse3.so.3 symlink), so a binary linked
      # against one can't dynamically load the other. Confirmed by testing:
      # a Debian-12-built binary's Depends: isn't even resolvable on
      # Debian 13. SQLCipher/OpenSSL are statically bundled in every build
      # either way (see Cargo.toml), so libfuse3 is the only runtime library
      # dependency that actually varies by target.
      - name: Install cargo-deb
        run: cargo install cargo-deb --locked --version "$CARGO_DEB_VERSION"

      - name: Build man page
        run: make man

      - name: Build .deb
        run: cargo deb

      - name: Rename package with distro id
        run: |
          SRC=$(ls target/debian/*.deb | head -1)
          DST="target/debian/$(basename "$SRC" | sed "s/_amd64/_${{ matrix.id }}_amd64/")"
          mv "$SRC" "$DST"
          echo "DEB_PATH=$DST" >> "$GITHUB_ENV"

      - uses: actions/upload-artifact@v7
        with:
          name: deb-${{ matrix.id }}
          path: ${{ env.DEB_PATH }}
          if-no-files-found: error

  verify:
    needs: build
    strategy:
      fail-fast: false
      matrix:
        include:
          - id: debian12
            image: debian:12-slim
          - id: debian13
            image: debian:13-slim
          - id: ubuntu2404
            image: ubuntu:24.04
          - id: ubuntu2604
            image: ubuntu:26.04
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v8
        with:
          name: deb-${{ matrix.id }}

      # Run the test distro as a plain `docker run` step rather than a job-
      # level `container:` - GitHub's job-container wrapper silently drops
      # (or otherwise doesn't honor) --security-opt on hosted runners: with
      # only --device/--cap-add set via `container: options:`, mount() was
      # still rejected with "Permission denied" by Docker's default seccomp
      # profile, identically before and after adding --security-opt
      # seccomp=unconfined there. A manual `docker run` (same approach as
      # packaging/test-install.sh, which works locally) gives full control
      # over every flag.
      #
      # A real `apt-get install`, not `dpkg -i`, so declared Depends:
      # actually have to resolve against this distro's package archive -
      # that's the whole point of testing per-distro in the first place.
      # Full functional smoke test, not just "did dpkg accept it": create a
      # container, mount it, write and read a file back, unmount, and run
      # the integrity check - proves FUSE mounting and SQLCipher both
      # actually work in this distro's environment, not just that the
      # binary is present.
      - name: Install and smoke-test the package the same way an end user would
        run: |
          docker run --rm \
            --cap-add SYS_ADMIN --device /dev/fuse --security-opt seccomp=unconfined \
            -v "$PWD:/work:ro" \
            ${{ matrix.image }} \
            bash -euxc '
              apt-get update -qq
              DEBIAN_FRONTEND=noninteractive apt-get install -y -qq /work/cryptc_*.deb

              mkdir -p /root/mnt
              printf "testpass\ntestpass\n" | cryptc create /root/vault.cryptc
              printf "testpass\n" | cryptc mount /root/vault.cryptc /root/mnt --foreground &
              MOUNT_PID=$!
              sleep 1
              echo "hello from CI" > /root/mnt/f.txt
              grep -q "hello from CI" /root/mnt/f.txt
              fusermount3 -u /root/mnt
              wait "$MOUNT_PID" 2>/dev/null || true
              printf "testpass\n" | cryptc check /root/vault.cryptc
            '

  release:
    needs: verify
    # Tag pushes (vX.Y.Z) publish a proper, stable release. Pushes to main
    # additionally republish a single rolling "latest" prerelease so a
    # built .deb is always available between tagged versions - see the
    # "Determine release identity" step for how the two are told apart.
    if: startsWith(github.ref, 'refs/tags/v') || github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v8
        with:
          path: artifacts
          merge-multiple: true

      - name: Determine release identity
        id: rel
        run: |
          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            echo "tag=$GITHUB_REF_NAME" >> "$GITHUB_OUTPUT"
            echo "title=$GITHUB_REF_NAME" >> "$GITHUB_OUTPUT"
            echo "prerelease=false" >> "$GITHUB_OUTPUT"
            echo "notes=cryptc $GITHUB_REF_NAME - one .deb per target distro (Debian 12/13, Ubuntu 24.04/26.04), see SHA256SUMS" >> "$GITHUB_OUTPUT"
          else
            echo "tag=latest" >> "$GITHUB_OUTPUT"
            echo "title=cryptc (main, rolling build)" >> "$GITHUB_OUTPUT"
            echo "prerelease=true" >> "$GITHUB_OUTPUT"
            echo "notes=Rolling build of main@${GITHUB_SHA::7} - not a stable release, see the tagged vX.Y.Z releases for that. One .deb per target distro (Debian 12/13, Ubuntu 24.04/26.04), see SHA256SUMS" >> "$GITHUB_OUTPUT"
          fi

      - name: Create or update the GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          cd artifacts
          sha256sum *.deb > SHA256SUMS
          TAG="${{ steps.rel.outputs.tag }}"
          PRERELEASE_FLAG=""
          [[ "${{ steps.rel.outputs.prerelease }}" == "true" ]] && PRERELEASE_FLAG="--prerelease"
          # "latest" is a moving target: delete the previous release+tag first
          # so the tag actually advances to this commit instead of staying
          # pinned to whichever commit first created it. Tagged vX.Y.Z
          # releases are never deleted here - only re-uploaded via the
          # `gh release upload --clobber` fallback below on a re-run.
          if [[ "$TAG" == "latest" ]]; then
            gh release delete "$TAG" --repo "$GITHUB_REPOSITORY" --yes --cleanup-tag || true
          fi
          gh release create "$TAG" \
            --repo "$GITHUB_REPOSITORY" \
            --title "${{ steps.rel.outputs.title }}" \
            --notes "${{ steps.rel.outputs.notes }}" \
            $PRERELEASE_FLAG \
            * \
          || gh release upload "$TAG" * \
            --repo "$GITHUB_REPOSITORY" --clobber
