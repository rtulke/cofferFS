.venv/
.claude/
CLAUDE.md
__pycache__/
*.pyc
*.cryptc
*.cryptc-wal
*.cryptc-shm
/target/
/target-*/
dist/
*.deb
build.log

# Python reference implementation: kept locally, not published to GitHub -
# only the Rust port (now at the repo root) is tracked and released.
/cryptc
/requirements.txt
/setup-python.sh
