.venv/
.claude/
CLAUDE.md
__pycache__/
*.pyc
*.coffer
*.coffer-wal
*.coffer-shm
/target/
/target-*/
dist/
*.deb
build.log

# Python reference implementation: kept locally, not published to GitHub -
# only the Rust port (now at the repo root) is tracked and released. Named
# distinctly (not just "coffer") so it can never shadow/be confused with
# the actual target/release/coffer or installed binary at the repo root.
/coffer-reference.py
/requirements.txt
/setup-python.sh
